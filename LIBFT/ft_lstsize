#include "libft.h"
/*
typedef struct s_list
{
    void            *content;
    struct s_list   *next;
} t_list;

t_list *ft_lstnew(void *content)
{
    t_list *new_node;

    new_node = (t_list *)malloc(sizeof(t_list));
    if (!new_node)
        return (NULL);
    new_node->content = content;
    new_node->next = NULL;
    return (new_node);
}
*/
int ft_lstsize(t_list *lst)
{
    t_list *tmpnode;
    int i;
    
    tmpnode = lst;
    i = 0;
    while (tmpnode)
    {
        tmpnode = tmpnode->next;
        i++;
    }
    return (i);
}
/*
int main(void)
{
    t_list *testone;
    t_list *testtwo;
    
    testone = ft_lstnew("ONE");
    testtwo = ft_lstnew("TWO");
    
    testone->next = testtwo;
    
    int size = ft_lstsize(testone);
    printf("%d", size);
}
*/
